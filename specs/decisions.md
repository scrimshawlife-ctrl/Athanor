# Decision register — completion packages 1-3

Status: advisory. This does not govern or activate. Owners below are roles from the existing constitution, not new authorities.

| ID | Question / required evidence | Proposed disposition | Owner / blocks |
|---|---|---|---|
| DEC-001 | Do the existing 400 GOLD rows have reviewed label decisions bound to their exact contents? | NOT_COMPUTABLE. Preserve the historical report; do not certify label correctness from heuristic selection or a hard-coded operator string. Obtain reviewed IDs, hashes, labels, and approval scope. | Operator; gold certification and learned metrics |
| DEC-002 | Which exact base checkpoint, tokenizer revision, and T0/T1 tier will be implemented? | MiniLM/ModernBERT remain provisional classes. Record immutable revisions and licenses before architecture freeze. | Operator with FORGE; encoder implementation |
| DEC-003 | How is scoped approval authenticated and checked by an executor? | The approval-reference contract records existing human decisions; it is not a capability token. Trusted verification mechanism remains NOT_COMPUTABLE until selected. A caller-supplied file/string alone is insufficient. | Operator; mutating workflow implementation |
| DEC-004 | Which languages, corpus size, latency, and memory budgets are supported? | Existing ranker tokenizes ASCII letters/digits. Proposed behavior rejects tokenless queries without hits; expanded Unicode support and measured budgets remain NOT_COMPUTABLE. | Operator/product owner; multilingual/performance claims |
| DEC-005 | Which duplicate-work grouping and split ratios are approved? | Group shared work/edition/source and content-hash components before deterministic assignment. Ratios, seed, canonicalizer, and grouping revision must be explicit freeze inputs, not hidden defaults. | Dataset reviewer; WF-005 |
| DEC-006 | What are metric support minima and uncertainty reporting rules? | E1/E2 require strict improvement over frozen controls; E3 requires non-regression for an improvement claim. Gold support floor, tie policy for exact matches, and uncertainty method need recorded selection. | Evaluation reviewer; measured quality claims |
| DEC-007 | Which receipt/query retention period and public-export jurisdictions apply? | Minimize query logging; no raw queries by default. Retention durations and jurisdiction-specific rights evidence remain NOT_COMPUTABLE. The historical year cutoff is a project HOLD heuristic, not proof of rights. | Operator/reviewer; policy-dependent retention or export |
| DEC-008 | How are ambiguous legacy reception values migrated? | Proposed aliases: period_translation → pd_translation; primary_pd → primary_witness only after witness review. Unknown becomes null with a reason, never fabricated certainty. | Dataset reviewer; migration |

Resolved for this documentation scope by the operator's continuation and approval: use current GitHub evidence for current commit/visibility/committed fixtures; preserve dated Notion history; distinguish all INFERRED rows from eligible KEEP; qualify unavailable gold/E7 proof; prepare packages 1-3 and factual Notion reconciliation. This approval does not supply missing dataset review or model measurements.

Decisions must record evidence and dependent IDs when closed. An unresolved choice may be fully specified as an input requirement; it cannot silently become an implementation default. Packages 4-5 will connect this register to tasks and verification evidence.

Provenance: Notion Sprint 001 Hub [not inspected; Athanor Hub inspected] + Loop 805 Slice N/A + Hash: fd84c7085579f3e7a560b38fc554a4f832fa9c10 (review base)
